
# Client libraries

The client library repository is available [here](https://github.com/googleapis/google-api-php-client). API specific references are below:

- [Account Management API](https://github.com/googleapis/google-api-php-client-services/tree/master/src/Google/Service/MyBusinessAccountManagement) 

<!--
Execute the following to install the client library. The repository is available [here](https://github.com/googleapis/google-api-php-client).

```
wget -O composer-setup.php https://getcomposer.org/installer
mkdir bin
php composer-setup.php --install-dir=bin --filename=composer
php bin/composer require google/apiclient:"^2.9.1" 
```
The library files are below for reference:
- [Account Management](https://github.com/googleapis/google-api-php-client-services/tree/master/src/Google/Service/MyBusinessAccountManagement)

# Prerequisites
## Get your client secrets

**Note**: You will need to have created a Client ID in your [Google Cloud Platform](https://cloud.google.com/console) before proceeding.

- Click the name of a Client ID to view that ID and download JSON</li>
- Create a new file called `client_secrets.json` in the same directory as source files.

# Run

`php -S localhost:8000 accounts-list.php`
-->

